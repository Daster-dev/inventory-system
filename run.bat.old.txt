@echo off
title تشغيل نظام إدارة المخزون

:: الانتقال إلى مجلد المشروع
cd /d "%~dp0"

:: التحقق من وجود Node.js
where node >nul 2>&1
if %errorlevel% neq 0 (
  echo ❌ Node.js غير مثبت، الرجاء تثبيته من https://nodejs.org
  timeout /t 5
  exit /b
)

:: تثبيت الحزم إذا لم تكن موجودة
if not exist "node_modules" (
  echo 🚀 جاري تثبيت الحزم...
  call npm install
)

:: تشغيل السيرفر بصمت كعملية مستقلة
start "" /min cmd /c "npm start"

:: الانتظار قليلًا ثم فتح المتصفح
timeout /t 2 /nobreak >nul
start "" "http://localhost:3000"

:: إنهاء نافذة البات بعد التشغيل
exit /b
